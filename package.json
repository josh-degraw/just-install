{
  "name": "just-install",
  "version": "1.0.0",
  "description": "Small utility tool for installing using yarn or npm, whichever is already used",
  "main": "dist/index.js",
  "author": "Josh DeGraw",
  "license": "MIT",
  "bin": {
    "just-install": "bin/just-install"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "format": "prettier src/*.ts --write"
  },
  "dependencies": {
    "esm": "^3.2.25",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2"
  },
  "keywords": [
    "install"
  ]
}